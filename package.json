{
  "name": "@bacons/mdx-repo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "lerna run build --parallel -- --watch",
    "prepare": "lerna run prepare --parallel"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "resolutions": {
    "expo": "^51",
    "fs-extra": "^9.1.0"
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "lerna": "3.4.1",
    "prettier": "^2.2.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.2.4",
    "expo-module-scripts": "^3.5.2",
    "@types/jest": "^26",
    "jest": "^26.6.3"
  }
}
